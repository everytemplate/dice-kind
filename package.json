{
  "name": "dice-kind",
  "version": "0.1.0",
  "description": "A demo Kind contract for dice written in AssemblyScript",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:kind": "kasc assembly/index.ts --out build/index.wasm --text build/index.wat",
    "register:matter": "every matter register build/index.wasm elements/data.json --json register/matter.json",
    "register:kind": "every kind register $(every pick matter.hash=build/index.wasm matter.hash=elements/data.json) --elements build/index.wasm --json register/kind.json",
    "register:all": "run-s 'register:matter -- {@}' 'register:kind -- {@}' --",
    "preregister:matter": "npm run build:kind"
  },
  "dependencies": {
    "@everyprotocol/kind-as": "^0.1.4",
    "npm-run-all2": "^8.0.4"
  }
}
